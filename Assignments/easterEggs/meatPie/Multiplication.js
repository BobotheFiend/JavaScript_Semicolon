const prompt = require("prompt-sync")();

let firstNumber = 10
let secondNumber = 11

let multiplyThemUp = firstNumber * secondNumber;

console.log(` ${firstNumber} x ${secondNumber} = ${multiplyThemUp}`)
