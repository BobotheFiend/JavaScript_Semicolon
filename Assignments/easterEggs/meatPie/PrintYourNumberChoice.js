const prompt = require("prompt-sync")();

let firstNumber = prompt("Enter a number: ")
let secondNumber = prompt("Enter another Number: ")


console.log(` Your first choice = ${firstNumber} &  Second Choice = ${secondNumber} `)
